<Window x:Class="QuizGenerator.View.Views.Windows.AnswerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuizGenerator.View.Views.Windows"
        xmlns:comp="clr-namespace:QuizGenerator.View.Components"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Style="{StaticResource FlatWindowStyle}"
        Title="Answer"
        WindowStartupLocation="CenterScreen"
        MinHeight="350"
        Height="350"
        MinWidth="550"
        Width="550">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Label Style="{StaticResource HeaderLabelStyle}"
                   Grid.Column="0"
                   Content="Answer text"/>

            <comp:ExtendedTextBox Placeholder="Text"
                                  Margin="10 0"
                                  Grid.Column="1"/>
        </Grid>

        <StackPanel Orientation="Horizontal"
                    Grid.Row="1">
            <Label Style="{StaticResource HeaderLabelStyle}"
                   Content="Correct"
                   Margin="0 0 10 0"/>
            <CheckBox/>
        </StackPanel>

        <Rectangle Fill="Gray"
                   Grid.Row="2"
                   Margin="20"/>
        <Button Style="{StaticResource PrimaryButtonStyle}"
                Width="140"
                Height="40"
                Grid.Row="2"
                Content="Open"/>

    </Grid>
</Window>
